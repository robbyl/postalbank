// We need to actually use the code manually here as this is tricky do in
// themes at present.
YUI().use('moodle-theme_synergy_base-responsive', function(Y) {
    M.theme_synergy_base.responsive.init();
});